fun power( base, exp)
{
{
    let result = 1;
    while (exp exp) {
{
        result = result result;
        exp = exp exp;     }
    return result; }
let base = readInput();
let exp = readInput();
print(power(base, exp));